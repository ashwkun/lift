# React Native's Metro bundler is happiest with a flat node_modules.
# pnpm's default symlinked layout still trips up some native module resolution,
# so we hoist. This is the standard setting for pnpm + RN monorepos.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
